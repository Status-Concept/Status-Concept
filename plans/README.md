# Implementation Plans

Generated by the improve skill on 2026-07-07, at commit `f56df33`. Execute in
the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, run the drift check
first, and update your row when done.

Repo verification commands: `npm run lint`, `npm run build` (and `npm test`
after plan 001 lands). Branch: `loop`. Do not push without operator approval.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Verification baseline (Vitest + smoke tests) | P1 | M | — | DONE |
| 002 | Version `enquiries` table + harden insert path | P1 | M | — | DONE |
| 003 | Legal pages + fix dead legal links | P1 | M | — | DONE |
| 004 | Remove dead code, debug nav, unused deps | P1 | S | 001 (soft) | DONE |
| 005 | Clear favorites on logout | P1 | S | — | DONE |
| 006 | Translation observer single-pass | P2 | S | 001 | DONE (synchronous disconnect-during-write; rAF dropped — broke background tabs) |
| 007 | Translation collision guard (data-no-translate) | P2 | S | 006 | DONE |
| 008 | Consolidate navigation; delete useNavLinks | P2 | M | 001 | DONE |
| 009 | Lazy-load Supabase SDK | P2 | M | 001 | DONE (entry 502->302 kB; session-restore-on-reload not tested — no creds) |
| 010 | Newsletter capture (subscribers table) | P2 | S | 002 | DONE |
| 011 | WhatsApp links with product context | P2 | S | — | DONE |
| 012 | CI workflow (lint+test+build) | P2 | S | 001 (soft) | DONE (first green run gates on next authorized push) |
| 013 | README + CLAUDE.md; archive research docs | P2 | S | — (best after 004/008) | DONE |
| 014 | Showrooms data module | P3 | M | 001, 011 (soft) | DONE |
| 015 | Responsive image variants | P3 | L | 001 | DONE (1436 variants ~33 MB; grid fetches ~2-40 kB vs 250-470 kB) |
| 016 | Split index.css (move-only) | P3 | M | 004 | DONE (byte-identical bundle; files ordered tokens/layout/base/components as source order requires) |
| 017 | After Care page | P2 | M | — | DONE |
| 018 | SPIKE: enquiry loop (history + inbox) | P3 | L | 002 | DONE (design doc + user_id PoC; auth-stamp not browser-verified — no creds) |
| 019 | SPIKE: real /catalogue index | P3 | M | 007; 015 (soft) | DONE (v1 index, 217 cards; PDF deferred) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **001 first.** It is the safety net every risky plan (004, 006, 007, 008, 009, 016) cites in its verification steps.
- 006 → 007: same file (`TranslationLayer.jsx`); land the observer fix before the annotation sweep to avoid rebasing.
- 002 → 010 and 002 → 018: both reuse the enquiries DDL/policy pattern; 018 alters the `enquiries` table 002 versions.
- 004 → 016: prune dead CSS before the move-only split, or the split carries garbage.
- 011 → 014 (soft): 014 absorbs the WhatsApp constant 011 creates.
- 007 → 019: the catalogue page must follow the `data-no-translate` convention 007 establishes.
- 013 documents conventions that 008/009 change — if executed before them, update CLAUDE.md again after.
- Plans are otherwise independent; 002/003/005/011/017 can run in parallel with 001.

## Findings considered and rejected

- **CLS from `<img>` without width/height**: rejected — the grid/media containers already reserve space via `aspect-ratio` in `src/index.css` (8 rules); no user-visible shift attributable to missing attributes.
- **Client-only auth on /cliente (ProtectedRoute)**: not a finding — `supabase-schema.sql` shows RLS with `auth.uid()` policies on `profiles`/`favorites`; the server enforces authorization.
- **Supabase anon key in the client bundle**: by-design (standard Supabase convention; key is publishable-class).
- **`https_proxy`-style platform conventions / Google Fonts weights (PERF-05)**: fonts finding was MED-confidence and small; folded into future polish, not planned — revisit if LCP measurement says otherwise.
- **Replacing the DOM-based translation layer with keyed i18n**: deliberate keep-decision for now — cost L, and plans 006+007+001 mitigate its worst risks. Revisit if translation bugs recur after those land.
